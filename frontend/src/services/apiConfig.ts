const apiUrl = "http://localhost:5237/";

const config = {
  semesterApiUrl: `${apiUrl}/Semester`,
  courseApiUrl: `${apiUrl}/Course`,
  assignmentApiUrl: `${apiUrl}/Assignment`,
};

export default config;
